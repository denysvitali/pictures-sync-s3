name: ota-image

concurrency:
  group: ota-image-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - master
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build-camera-kernel:
    name: Build FAT/exFAT camera kernel
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: false

      - name: Cache camera kernel
        id: cache-camera-kernel
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/dist/camera-kernel-armv6
          key: ${{ runner.os }}-${{ runner.arch }}-camera-kernel-armv6-${{ hashFiles('.github/scripts/build-camera-kernel-armv6.sh', '.github/patches/kernel-enable-camera-filesystems.patch') }}

      - name: Build FAT/exFAT-enabled ARMv6 kernel
        if: steps.cache-camera-kernel.outputs.cache-hit != 'true'
        run: bash .github/scripts/build-camera-kernel-armv6.sh

      - name: Validate camera kernel package
        run: |
          test -s dist/camera-kernel-armv6/dist/vmlinuz
          test -f dist/camera-kernel-armv6/dist/bcm2708-rpi-b-plus.dtb
          test -f dist/camera-kernel-armv6/dist/bcm2711-rpi-4-b.dtb

      - name: Upload camera kernel package
        uses: actions/upload-artifact@v7
        with:
          name: camera-kernel-armv6
          path: dist/camera-kernel-armv6
          if-no-files-found: error

  test:
    name: Run Go tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.33.2

      - name: Build embedded webui bundle
        run: |
          cd webui
          corepack enable
          pnpm install --frozen-lockfile
          pnpm build

      - name: Run tests
        run: go test -short ./...

  build-and-publish-flash-image:
    needs:
      - test
      - build-camera-kernel
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 45
    env:
      GOKRAZY_INSTANCE: photo-backup
      GOKRAZY_PARENT_DIR: ${{ github.workspace }}/gokrazy
      GOKRAZY_REPOSITORY: denysvitali/gokrazy
      GOKRAZY_REF: cf5d4d8891039b441392ea3676f594e6d9588477
      GOKRAZY_TOOLS_REPOSITORY: denysvitali/gokrazy-tools
      GOKRAZY_TOOLS_REF: 242f3106842c380a1c9a4a4473e3e3c7090f29f2
      GOKRAZY_INTERNAL_REPOSITORY: denysvitali/gokrazy-internal
      GOKRAZY_INTERNAL_REF: 516186dbfc01bdae7730e451d2b8a7c7a24e5452
      IMAGE_NAME: photo-backup-rpi.img
      TARGET_STORAGE_BYTES: 2147483648
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.33.2

      - name: Build embedded webui bundle
        run: |
          cd webui
          corepack enable
          pnpm install --frozen-lockfile || pnpm install
          pnpm build
          if [ ! -f ../pkg/webui/dist/index.html ]; then
            echo "::error::pkg/webui/dist/index.html is missing after webui build"
            exit 1
          fi

      - name: Cache gokrazy CLI
        id: cache-gok
        uses: actions/cache@v5
        with:
          path: ~/go/bin/gok
          key: ${{ runner.os }}-${{ runner.arch }}-gokrazy-gok-${{ env.GOKRAZY_TOOLS_REPOSITORY }}-${{ env.GOKRAZY_TOOLS_REF }}-${{ env.GOKRAZY_INTERNAL_REPOSITORY }}-${{ env.GOKRAZY_INTERNAL_REF }}-${{ hashFiles('.github/patches/gokrazy-tools-copy-tryboot-files.patch') }}

      - name: Install gokrazy CLI
        if: steps.cache-gok.outputs.cache-hit != 'true'
        run: |
          git clone --depth=1 "https://github.com/${GOKRAZY_TOOLS_REPOSITORY}.git" /tmp/gokrazy-tools
          git -C /tmp/gokrazy-tools fetch --depth=1 origin "${GOKRAZY_TOOLS_REF}"
          git -C /tmp/gokrazy-tools checkout --detach FETCH_HEAD
          git -C /tmp/gokrazy-tools apply --check "${{ github.workspace }}/.github/patches/gokrazy-tools-copy-tryboot-files.patch"
          git -C /tmp/gokrazy-tools apply "${{ github.workspace }}/.github/patches/gokrazy-tools-copy-tryboot-files.patch"

          git clone --depth=1 "https://github.com/${GOKRAZY_INTERNAL_REPOSITORY}.git" /tmp/gokrazy-internal
          git -C /tmp/gokrazy-internal fetch --depth=1 origin "${GOKRAZY_INTERNAL_REF}"
          git -C /tmp/gokrazy-internal checkout --detach FETCH_HEAD

          go -C /tmp/gokrazy-tools mod edit -replace=github.com/gokrazy/internal=/tmp/gokrazy-internal
          go -C /tmp/gokrazy-tools install ./cmd/gok

      - name: Checkout Gokrazy runtime fork
        run: |
          git clone --depth=1 "https://github.com/${GOKRAZY_REPOSITORY}.git" /tmp/gokrazy
          git -C /tmp/gokrazy fetch --depth=1 origin "${GOKRAZY_REF}"
          git -C /tmp/gokrazy checkout --detach FETCH_HEAD

      - name: Add Go bin to PATH
        run: |
          echo "PATH=$PATH:$(go env GOPATH)/bin" >> "$GITHUB_ENV"

      - name: Download FAT/exFAT camera kernel
        uses: actions/download-artifact@v8
        with:
          name: camera-kernel-armv6
          path: ${{ github.workspace }}/dist/camera-kernel-armv6

      - name: Validate downloaded camera kernel
        run: |
          test -s dist/camera-kernel-armv6/dist/vmlinuz
          test -f dist/camera-kernel-armv6/go.mod

      - name: Cache static ARMv6 hostapd
        id: cache-hostapd
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/dist/hostapd-armv6/hostapd
          key: ${{ runner.os }}-${{ runner.arch }}-hostapd-armv6-${{ hashFiles('.github/scripts/build-hostapd-armv6.sh') }}

      - name: Build static ARMv6 hostapd
        if: steps.cache-hostapd.outputs.cache-hit != 'true'
        run: |
          bash .github/scripts/build-hostapd-armv6.sh

      - name: Resolve static ARMv6 hostapd
        id: hostapd
        shell: bash
        run: |
          hostapd_binary="${{ github.workspace }}/dist/hostapd-armv6/hostapd"
          if [ ! -x "$hostapd_binary" ]; then
            echo "::error::static ARMv6 hostapd binary is missing or not executable: $hostapd_binary"
            exit 1
          fi
          file "$hostapd_binary"
          if ! file "$hostapd_binary" | grep -q 'ARM, EABI5'; then
            echo "::error::cached hostapd is not a 32-bit ARM EABI5 binary"
            exit 1
          fi
          if ! file "$hostapd_binary" | grep -q 'statically linked'; then
            echo "::error::cached hostapd is not statically linked"
            exit 1
          fi
          echo "hostapd_binary=$hostapd_binary" >> "$GITHUB_OUTPUT"

      - name: Cache static ARMv6 exfatprogs
        id: cache-exfatprogs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/dist/exfatprogs-armv6/mkfs.exfat
          key: ${{ runner.os }}-${{ runner.arch }}-exfatprogs-armv6-${{ hashFiles('.github/scripts/build-exfatprogs-armv6.sh') }}

      - name: Build static ARMv6 exfatprogs
        if: steps.cache-exfatprogs.outputs.cache-hit != 'true'
        run: |
          bash .github/scripts/build-exfatprogs-armv6.sh

      - name: Resolve static ARMv6 mkfs.exfat
        id: exfatprogs
        shell: bash
        run: |
          exfat_mkfs_binary="${{ github.workspace }}/dist/exfatprogs-armv6/mkfs.exfat"
          if [ ! -x "$exfat_mkfs_binary" ]; then
            echo "::error::static ARMv6 mkfs.exfat binary is missing or not executable: $exfat_mkfs_binary"
            exit 1
          fi
          file "$exfat_mkfs_binary"
          if ! file "$exfat_mkfs_binary" | grep -q 'ARM, EABI5'; then
            echo "::error::cached mkfs.exfat is not a 32-bit ARM EABI5 binary"
            exit 1
          fi
          if ! file "$exfat_mkfs_binary" | grep -q 'statically linked'; then
            echo "::error::cached mkfs.exfat is not statically linked"
            exit 1
          fi
          echo "exfat_mkfs_binary=$exfat_mkfs_binary" >> "$GITHUB_OUTPUT"

      - name: Cache static ARMv6 e2fsprogs
        id: cache-e2fsprogs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/dist/e2fsprogs-armv6/mke2fs
          key: ${{ runner.os }}-${{ runner.arch }}-e2fsprogs-armv6-${{ hashFiles('.github/scripts/build-mke2fs-armv6.sh') }}

      - name: Build static ARMv6 e2fsprogs
        if: steps.cache-e2fsprogs.outputs.cache-hit != 'true'
        run: |
          bash .github/scripts/build-mke2fs-armv6.sh

      - name: Resolve static ARMv6 mke2fs
        id: e2fsprogs
        shell: bash
        run: |
          mke2fs_binary="${{ github.workspace }}/dist/e2fsprogs-armv6/mke2fs"
          if [ ! -x "$mke2fs_binary" ]; then
            echo "::error::static ARMv6 mke2fs binary is missing or not executable: $mke2fs_binary"
            exit 1
          fi
          file "$mke2fs_binary"
          if ! file "$mke2fs_binary" | grep -q 'ARM, EABI5'; then
            echo "::error::cached mke2fs is not a 32-bit ARM EABI5 binary"
            exit 1
          fi
          if ! file "$mke2fs_binary" | grep -q 'statically linked'; then
            echo "::error::cached mke2fs is not statically linked"
            exit 1
          fi
          echo "mke2fs_binary=$mke2fs_binary" >> "$GITHUB_OUTPUT"

      - name: Compute release tag
        id: release-tag
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == "refs/heads/master" ]]; then
            calver="v$(date -u +'%Y.%-m.%-d.%H%M')"
            echo "tag=${calver}" >> "$GITHUB_OUTPUT"
            echo "name=Photo Sync OTA ${calver}" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          elif [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "name=Photo Sync OTA ${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          else
            echo "tag=" >> "$GITHUB_OUTPUT"
            echo "name=" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build flashable Gokrazy image
        id: build
        env:
          GOKRAZY_INSTANCE: ${{ env.GOKRAZY_INSTANCE }}
          GOKRAZY_PARENT_DIR: ${{ env.GOKRAZY_PARENT_DIR }}
          IMAGE_DIR: ${{ github.workspace }}/ota
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          GOKRAZY_IMAGE_MODE: full
          TARGET_STORAGE_BYTES: ${{ env.TARGET_STORAGE_BYTES }}
          HOSTAPD_BINARY: ${{ steps.hostapd.outputs.hostapd_binary }}
          EXFAT_MKFS_BINARY: ${{ steps.exfatprogs.outputs.exfat_mkfs_binary }}
          MKE2FS_BINARY: ${{ steps.e2fsprogs.outputs.mke2fs_binary }}
          GOKRAZY_MODULE_REPLACE: /tmp/gokrazy
          GOKRAZY_KERNEL_REPLACE: ${{ github.workspace }}/dist/camera-kernel-armv6
          TAG_NAME: ${{ steps.release-tag.outputs.tag }}
        run: |
          make ota

      - name: Build gokrazy root OTA image
        id: build-root
        env:
          GOKRAZY_INSTANCE: ${{ env.GOKRAZY_INSTANCE }}
          GOKRAZY_PARENT_DIR: ${{ env.GOKRAZY_PARENT_DIR }}
          IMAGE_DIR: ${{ github.workspace }}/ota
          IMAGE_NAME: photo-backup-rpi-root.squashfs
          BOOT_IMAGE_NAME: photo-backup-rpi-boot.fat
          GOKRAZY_TRYBOOT_KERNEL: "1"
          GOKRAZY_IMAGE_MODE: ota
          HOSTAPD_BINARY: ${{ steps.hostapd.outputs.hostapd_binary }}
          EXFAT_MKFS_BINARY: ${{ steps.exfatprogs.outputs.exfat_mkfs_binary }}
          MKE2FS_BINARY: ${{ steps.e2fsprogs.outputs.mke2fs_binary }}
          GOKRAZY_MODULE_REPLACE: /tmp/gokrazy
          GOKRAZY_KERNEL_REPLACE: ${{ github.workspace }}/dist/camera-kernel-armv6
          TAG_NAME: ${{ steps.release-tag.outputs.tag }}
        run: |
          make ota

      - name: Validate rollback-safe boot OTA image
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends mtools
          boot_image="${{ steps.build-root.outputs.boot_image_path }}"
          mtype -i "$boot_image" ::config.txt | grep -qx 'kernel=vmlinuz.recovery'
          mtype -i "$boot_image" ::tryboot.txt | grep -qx 'kernel=vmlinuz'
          mdir -i "$boot_image" ::vmlinuz
          mdir -i "$boot_image" ::vmlinuz.recovery

      - name: Compress flash image for release
        id: compress
        shell: bash
        run: |
          compressed_path="${{ steps.build.outputs.image_path }}.gz"
          gzip -9 -c "${{ steps.build.outputs.image_path }}" > "$compressed_path"
          echo "compressed_path=$compressed_path" >> "$GITHUB_OUTPUT"

      - name: Compress root OTA image for release
        id: compress-root
        shell: bash
        run: |
          compressed_path="${{ steps.build-root.outputs.image_path }}.gz"
          gzip -9 -c "${{ steps.build-root.outputs.image_path }}" > "$compressed_path"
          echo "compressed_path=$compressed_path" >> "$GITHUB_OUTPUT"

      - name: Compress boot OTA image for release
        id: compress-boot
        shell: bash
        run: |
          compressed_path="${{ steps.build-root.outputs.boot_image_path }}.gz"
          gzip -9 -c "${{ steps.build-root.outputs.boot_image_path }}" > "$compressed_path"
          echo "compressed_path=$compressed_path" >> "$GITHUB_OUTPUT"

      - name: Attest build provenance for squashfs OTA image
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: ${{ steps.build-root.outputs.image_path }}

      - name: Keep build artifact
        uses: actions/upload-artifact@v7
        with:
          name: pictures-sync-ota-flash-image
          path: |
            ${{ steps.build.outputs.image_path }}
            ${{ steps.compress.outputs.compressed_path }}
            ${{ steps.build-root.outputs.image_path }}
            ${{ steps.compress-root.outputs.compressed_path }}
            ${{ steps.build-root.outputs.boot_image_path }}
            ${{ steps.compress-boot.outputs.compressed_path }}
          if-no-files-found: error

      - name: Publish OTA release
        if: github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.release-tag.outputs.tag }}
          target_commitish: ${{ github.sha }}
          name: ${{ steps.release-tag.outputs.name }}
          prerelease: ${{ steps.release-tag.outputs.prerelease }}
          generate_release_notes: true
          files: |
            ${{ steps.compress.outputs.compressed_path }}
            ${{ steps.compress-root.outputs.compressed_path }}
            ${{ steps.compress-boot.outputs.compressed_path }}
